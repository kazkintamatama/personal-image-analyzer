import React from 'react';
import ReactDOM from 'react-dom/client';
import PersonalImageAnalyzer from './PersonalImageAnalyzer';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PersonalImageAnalyzer />
  </React.StrictMode>
);
